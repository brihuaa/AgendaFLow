// ============================================================
// AgendaFlow — Feature Flags (Frontend)
// Deben coincidir con los valores del backend (config.js).
// Con false: el módulo no aparece, no se hacen peticiones.
// ============================================================

export const ENABLE_ANALYTICS_MODULE = true;
export const ENABLE_REVIEWS = true;
